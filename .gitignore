# Environment variables
backend-go/.env
**/.env

# Python virtual environments & cache
venv/
agentic-npc/
.venv/
__pycache__/

# Go binaries
*.exe
myapp

# Go workspace vendoring — modules (go.work.sum / go.sum) make a committed vendor/ tree
# redundant; regenerate locally with `go work vendor` if an offline build is ever needed.
/vendor/

# IDE folders
.idea/
.vscode/.pytest_cache/
.ruff_cache/
